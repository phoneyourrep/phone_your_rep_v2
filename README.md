# Phone Your Rep Frontend V2

## Development
Run `npm install` to install packages.

Run `npm run dev-server` to launch the local dev server and watch for changes. Then go to [http://localhost:8080](http://localhost:8080).
